# Web app environment.
#
#   cp .env.example .env.local
#
# Only NEXT_PUBLIC_ values belong here: Next inlines them into the browser
# bundle, so anything placed in this file is public. Secrets that the frontend
# needs must be proxied through the API instead.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
