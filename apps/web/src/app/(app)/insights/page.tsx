import { PhasePlaceholder } from "@/components/phase-placeholder";

export default function InsightsPage() {
  return (
    <PhasePlaceholder
      title="Insights"
      phase={10}
      phaseName="Career Insights"
      description="What your search is telling you: recurring gaps, skill demand, and how your applications actually perform."
    />
  );
}
