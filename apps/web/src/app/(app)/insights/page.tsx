import { InsightsScreen } from "@/features/insights/insights";

/**
 * Insights.
 *
 * Skill demand and recurring gaps. Role analysis and the application funnel
 * are named for this phase in `docs/09` and are deliberately not here — see
 * `application/insights/demand.py` for which, and why.
 */
export default function InsightsPage() {
  return <InsightsScreen />;
}
