import { PhasePlaceholder } from "@/components/phase-placeholder";

export default function ApplicationsPage() {
  return (
    <PhasePlaceholder
      title="Applications"
      phase={8}
      phaseName="Application Tracker"
      description="Track where each application stands, with the exact resume you sent attached to it."
    />
  );
}
