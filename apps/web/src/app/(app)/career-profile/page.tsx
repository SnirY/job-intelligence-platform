import { PhasePlaceholder } from "@/components/phase-placeholder";

export default function CareerProfilePage() {
  return (
    <PhasePlaceholder
      title="Career Profile"
      phase={2}
      phaseName="Career Profile"
      description="Your skills, experience, projects, and evidence — the source of truth every match is measured against."
    />
  );
}
